from django.apps import AppConfig


class CommandsConfig(AppConfig):
    """A class to represent the `commands` app configuration."""

    default_auto_field = "django.db.models.BigAutoField"

    name = "apps.commands"
